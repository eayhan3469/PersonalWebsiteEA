# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Node (not used yet, but just in case)
node_modules/

# Original uncompressed source videos — kept locally, never deployed.
# Web-ready previews live in assets/video/{prototypes,work}/ and ARE committed.
_media-source/
